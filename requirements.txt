# Python 3.11-3.13 supported (all versions with pre-built wheels)

# Web Scraping & HTTP
httpx==0.27.2
urllib3==2.1.0

# Content Extraction & Parsing
trafilatura==1.6.1
beautifulsoup4==4.12.2
readability-lxml==0.8.1
lxml==5.2.2
markdown==3.5.1

# API Framework (fastapi 0.115.0 + pydantic 2.8+ for Python 3.13 support)
fastapi==0.115.0
uvicorn[standard]==0.30.6
orjson==3.10.7
pydantic==2.8.0

# Vector Search & Embeddings (pinned for reproducible builds)
sentence-transformers==2.6.1
faiss-cpu==1.12.0
numpy==2.1.3

# Reranking (cross-encoder) - optional for now
# torch==2.0.1
# transformers==4.34.0

# Full-text search & BM25
whoosh==2.7.4

# Utilities
python-dotenv==1.0.0
loguru==0.7.2
tqdm==4.66.1
pyyaml==6.0.1

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1

# Optional: for HyDE query generation (requires LLM)
# langchain==0.0.310  # Can add if needed for HyDE generation
